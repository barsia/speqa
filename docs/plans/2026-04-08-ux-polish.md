# UX Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix all UX issues found in the preview panel design review — hardcoded colors, inconsistent icons, missing focus states, and visual clutter.

**Architecture:** All changes are in existing UI files. Theme colors move to `SpeqaThemeColors`, icon sizes unify, focus/hover states improve. No new files, no model changes.

**Tech Stack:** Kotlin, Compose/Jewel UI, IntelliJ UIManager theme colors

---

### Task 1: Extract hardcoded colors to SpeqaThemeColors

**Files:**
- Modify: `src/main/kotlin/io/speqa/speqa/editor/ui/EditorPrimitives.kt`
- Modify: `src/main/kotlin/io/speqa/speqa/editor/ui/StepCard.kt`
- Modify: `src/main/kotlin/io/speqa/speqa/editor/ui/AttachmentRow.kt`
- Modify: `src/main/kotlin/io/speqa/speqa/editor/ui/StepsSection.kt`

Three hardcoded colors to extract:
- `Color(0xFFCC3333)` → `SpeqaThemeColors.destructive` (delete red, used in StepCard, AttachmentRow, InlineEditableIdRow)
- `Color(0xFF31A24C)` → `SpeqaThemeColors.commitFlash` (green flash, used in CommitFlashState)
- `Color(0xFF4A90D9)` → `SpeqaThemeColors.dropTarget` (drag indicator, used in StepsSection)

- [ ] **Step 1: Add three color tokens to SpeqaThemeColors in EditorPrimitives.kt**

In `SpeqaThemeColors` object, add after `actionHover`:

```kotlin
val destructive: Color
    get() = themeColor("Component.errorFocusColor", AwtColor(0xCC, 0x33, 0x33))

val commitFlash: Color
    get() = themeColor("Component.focusColor", AwtColor(0x31, 0xA2, 0x4C))

val dropTarget: Color
    get() = accent
```

Spec edit required before this `.kt` edit: add a line about semantic color tokens to the spec.

- [ ] **Step 2: Replace hardcoded delete red in StepCard.kt**

Replace `val deleteColor = Color(0xFFCC3333)` with `val deleteColor = SpeqaThemeColors.destructive`.

Spec edit required before this `.kt` edit.

- [ ] **Step 3: Replace hardcoded delete red in AttachmentRow.kt**

Replace `val deleteColor = Color(0xFFCC3333)` with `val deleteColor = SpeqaThemeColors.destructive`.

Spec edit required before this `.kt` edit.

- [ ] **Step 4: Replace hardcoded green in CommitFlashState in EditorPrimitives.kt**

In `CommitFlashState.greenTint()`, replace `Color(0xFF31A24C)` with `SpeqaThemeColors.commitFlash`.

Spec edit required before this `.kt` edit.

- [ ] **Step 5: Replace hardcoded red in InlineEditableIdRow in EditorPrimitives.kt**

Replace `Color(0xFFCC3333)` with `SpeqaThemeColors.destructive` (appears twice — unassigned ID text color and duplicate ID color).

Spec edit required before this `.kt` edit.

- [ ] **Step 6: Replace hardcoded drop target blue in StepsSection.kt**

Replace `androidx.compose.ui.graphics.Color(0xFF4A90D9)` with `SpeqaThemeColors.dropTarget`.

Spec edit required before this `.kt` edit.

- [ ] **Step 7: Replace hardcoded verdict colors in SpeqaThemeColors**

In `SpeqaThemeColors`, `verdictPassed`, `verdictFailed`, `verdictSkipped` use hardcoded colors. Replace:
- `verdictPassed`: derive from `commitFlash` — `commitFlash.copy(alpha = 0.08f)`
- `verdictFailed`: derive from `destructive` — `destructive.copy(alpha = 0.08f)`
- `verdictSkipped` can stay hardcoded (neutral gray, not a theme token)

Spec edit required before this `.kt` edit.

- [ ] **Step 8: Verify compilation**

Run: `./gradlew compileKotlin`
Expected: BUILD SUCCESSFUL

---

### Task 2: Unify delete icon sizes

**Files:**
- Modify: `src/main/kotlin/io/speqa/speqa/editor/ui/AttachmentRow.kt`

- [ ] **Step 1: Change AttachmentRow icon size from 14dp to 16dp**

In `AttachmentRow.kt`, change the trash icon `Modifier.size(14.dp)` to `Modifier.size(16.dp)`:

```kotlin
Icon(removeIcon, contentDescription = SpeqaBundle.message("tooltip.removeAttachment"), modifier = Modifier.size(16.dp), tint = deleteColor)
```

Also change the file icon from 14dp to 16dp for visual balance:

```kotlin
Icon(fileIcon, contentDescription = null, modifier = Modifier.size(16.dp), tint = SpeqaThemeColors.mutedForeground)
```

Spec edit required before this `.kt` edit.

- [ ] **Step 2: Verify compilation**

Run: `./gradlew compileKotlin`
Expected: BUILD SUCCESSFUL

---

### Task 3: Increase step number column width

**Files:**
- Modify: `src/main/kotlin/io/speqa/speqa/editor/ui/EditorPrimitives.kt`

- [ ] **Step 1: Change stepNumberColumnWidth from 20.dp to 24.dp**

In `SpeqaLayout`:

```kotlin
val stepNumberColumnWidth = 24.dp
```

Spec edit required before this `.kt` edit.

- [ ] **Step 2: Verify compilation**

Run: `./gradlew compileKotlin`
Expected: BUILD SUCCESSFUL

---

### Task 4: Hide empty attachments section header

**Files:**
- Modify: `src/main/kotlin/io/speqa/speqa/editor/ui/TestCasePreview.kt`

- [ ] **Step 1: Only show ATTACHMENTS header when attachments exist**

In `TestCasePreview.kt`, the current code:

```kotlin
onTestCaseChange?.let { onChange ->
    SectionHeaderWithDivider(SpeqaBundle.message("label.attachments"))
    AttachmentList(...)
```

Change to conditionally show the header:

```kotlin
onTestCaseChange?.let { onChange ->
    if (testCase.attachments.isNotEmpty()) {
        SectionHeaderWithDivider(SpeqaBundle.message("label.attachments"))
    }
    AttachmentList(...)
```

Spec edit required before this `.kt` edit.

- [ ] **Step 2: Verify compilation**

Run: `./gradlew compileKotlin`
Expected: BUILD SUCCESSFUL

---

### Task 5: Increase EditToggleIcon resting alpha

**Files:**
- Modify: `src/main/kotlin/io/speqa/speqa/editor/ui/EditorPrimitives.kt`

- [ ] **Step 1: Change alpha from 0.3f to 0.5f**

In `EditToggleIcon`, change:

```kotlin
val alpha = if (isEditing || isIconHovered) 1f else 0.3f
```

to:

```kotlin
val alpha = if (isEditing || isIconHovered) 1f else 0.5f
```

Also in `InlineEditableTitleRow`, change:

```kotlin
val titleIconAlpha = if (isEditing || flash.showFlash || isTitleIconHovered) 1f else 0.3f
```

to:

```kotlin
val titleIconAlpha = if (isEditing || flash.showFlash || isTitleIconHovered) 1f else 0.5f
```

Also in `InlineEditableIdRow` (view mode), change:

```kotlin
val idIconAlpha = if (flash.showFlash || isIdIconHovered) 1f else 0.3f
```

to:

```kotlin
val idIconAlpha = if (flash.showFlash || isIdIconHovered) 1f else 0.5f
```

Spec edit required before each `.kt` edit.

- [ ] **Step 2: Verify compilation**

Run: `./gradlew compileKotlin`
Expected: BUILD SUCCESSFUL

---

### Task 6: Fix tag chip remove hover color

**Files:**
- Modify: `src/main/kotlin/io/speqa/speqa/editor/ui/TagCloud.kt`

- [ ] **Step 1: Replace subtleBorder with actionHover for tag remove hover**

In `TagChip`, change:

```kotlin
val removeBg = if (isRemoveHovered) SpeqaThemeColors.subtleBorder else Color.Transparent
```

to:

```kotlin
val removeBg = if (isRemoveHovered) SpeqaThemeColors.actionHover else Color.Transparent
```

Spec edit required before this `.kt` edit.

- [ ] **Step 2: Verify compilation**

Run: `./gradlew compileKotlin`
Expected: BUILD SUCCESSFUL

---

### Task 7: Add focus ring to PlainTextInput

**Files:**
- Modify: `src/main/kotlin/io/speqa/speqa/editor/ui/EditorPrimitives.kt`

- [ ] **Step 1: Track focus state and change border color**

In `PlainTextInput`, add focus tracking:

```kotlin
var isFocused by remember { mutableStateOf(false) }
```

Update the border to use accent when focused:

```kotlin
val borderColor = when {
    readOnly -> Color.Transparent
    isFocused -> SpeqaThemeColors.accent
    else -> SpeqaThemeColors.border
}
```

Replace `.border(1.dp, if (readOnly) Color.Transparent else SpeqaThemeColors.border, ...)` with `.border(1.dp, borderColor, ...)`.

Add `.onFocusChanged { isFocused = it.isFocused }` to the modifier chain (before `.onPreviewKeyEvent`).

Spec edit required before this `.kt` edit.

- [ ] **Step 2: Verify compilation**

Run: `./gradlew compileKotlin`
Expected: BUILD SUCCESSFUL

---

### Task 8: Update spec document

**Files:**
- Modify: `docs/specs/2026-04-06-speqa-mvp-design.md`

- [ ] **Step 1: Add UX polish documentation to spec**

Add a bullet summarizing all the changes:
- Semantic color tokens: `destructive`, `commitFlash`, `dropTarget` in SpeqaThemeColors
- Unified delete icon sizes at 16dp
- Step number column width 24dp
- Attachments header hidden when empty
- Edit icon resting alpha 0.5
- Tag chip remove hover uses `actionHover`
- PlainTextInput focus ring with `accent` border

- [ ] **Step 2: Verify tests still pass**

Run: `./gradlew test`
Expected: All tests pass
